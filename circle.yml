machine:
  node:
    version: 6

dependencies:
  pre:
    - npm install selenium-standalone@latest -g
    - selenium-standalone install
    - selenium-standalone start & echo $! > selenium.pid

test:
  post:
  	- kill $(cat selenium.pid)
